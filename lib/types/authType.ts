export type LoginType = {
  email: string;
  password: string;
};

export type RegisterType = {
  fullname: string;
  email: string;
  password: string;
  poste: string;
};

export type activationType = {
  password: string;
  userId: string;
};
